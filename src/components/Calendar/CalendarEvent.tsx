import { CalendarEvent } from "@/types";

export interface CalendarEventCardProps {
  data: CalendarEvent;
}

export const CalendarEventCard = (props: CalendarEventCardProps) => {
  return <div>CalendarEventCard</div>;
};
